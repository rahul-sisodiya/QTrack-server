# QTrack Server Environment Example
# Copy to .env and fill values

# Port your Node server should listen on (platform may override)
PORT=4002

# MongoDB connection string for production/deployment
# Example (MongoDB Atlas):
# mongodb+srv://<username>:<password>@<cluster>.qtrack.mongodb.net/qtrack?retryWrites=true&w=majority&appName=<cluster>
MONGO_URI=

# Optional: Allow AI route (remove if unused)
# GEMINI_API_KEY=your_api_key
# GEMINI_MODEL=gemini-1.5-flash
# GEMINI_API_VERSION=v1

# CORS (optional). Comma-separated list of allowed origins, e.g., https://yourapp.vercel.app
# If not set, server uses permissive CORS. Set for production hardening.
ALLOWED_ORIGINS=